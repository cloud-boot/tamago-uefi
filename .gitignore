# build artifacts
app.elf
app_*.elf
*.efi
*.EFI
*.iso
esp.img
vars.fd
startup.nsh
*.log
# Host-side helper binaries — built into the package dir by
# `go build ./cmd/...`; never committed.
/cmd/blkprintk-seed/blkprintk-seed
/cmd/blkprintk-recover/blkprintk-recover
/blkprintk-seed
/blkprintk-recover
# Phase-3 sprint-2C genufs outputs — image is ~64 MiB and is rebuildable
# via `bash internal/livefreebsdboot/genufs/genufs.sh` (see that dir's README).
ufs2-fresh.img
*.ufs.img
/internal/livefreebsdboot/genufs/verify/verify
# init-src build artifacts: per-arch static ELF /init binaries + stage dirs
# (the cpio.gz that gets embedded IS committed; the loose ELF + stage tree
# are rebuildable via internal/embed_initramfs/init_src/build.sh)
internal/embed_initramfs/init_src/init.*
internal/embed_initramfs/init_src/root_*/
# Test runner output binaries
/internal/livefreebsdboot/buildespimg/buildespimg
# Sibling worktree from sprint 1.1 (R-amd64f isolation); never committed
/tamago-uefi
