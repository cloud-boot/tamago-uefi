# The chained EFI payloads (gzipped via M6.1 embed compression)
# are regenerated by the Taskfile every build; do not commit them.
# The Go source files (`chained_*.go`, `decompress.go`) are the
# only artifacts checked in here.
chained_amd64.efi.gz
chained_arm64.efi.gz
chained_riscv64.efi.gz
chained_loong64.efi.gz
